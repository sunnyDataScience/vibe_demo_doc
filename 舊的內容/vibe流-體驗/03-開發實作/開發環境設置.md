# 🛠️ 開發環境設置：工欲善其事，必先利其器

## 🎯 為什麼需要設置開發環境？
就像廚師需要準備廚具，木工需要準備工具一樣，寫程式也需要準備開發工具。好的工具讓開發事半功倍！

## 📋 必需工具清單

### 1. 代碼編輯器 (必裝)
**推薦：Visual Studio Code (VS Code)**
- 免費且功能強大
- 支援多種程式語言
- 豐富的插件生態

**安裝步驟：**
1. 前往 https://code.visualstudio.com/
2. 下載對應作業系統版本
3. 依照指示安裝
4. 啟動並確認正常運作

**必裝插件：**
```
- Auto Rename Tag：自動重命名HTML標籤
- Bracket Pair Colorizer：括號配色
- Prettier：程式碼格式化
- Live Server：本地網頁服務器
- JavaScript (ES6) snippets：JS程式碼片段
```

### 2. Node.js 運行環境
**用途：**執行JavaScript程式、管理套件

**安裝步驟：**
1. 前往 https://nodejs.org/
2. 下載LTS版本（長期支持版）
3. 安裝完成後，開啟終端機/命令提示字元
4. 輸入 `node --version` 確認安裝成功
5. 輸入 `npm --version` 確認套件管理器正常

### 3. Git 版本控制
**用途：**追蹤程式碼變更、備份、協作

**安裝步驟：**
1. 前往 https://git-scm.com/
2. 下載對應作業系統版本
3. 安裝完成後，開啟終端機
4. 輸入 `git --version` 確認安裝成功

**初始設定：**
```bash
git config --global user.name \"你的姓名\"
git config --global user.email \"你的信箱\"
```

### 4. Chrome 瀏覽器 (開發者版本)
**用途：**測試Chrome插件、網頁偵錯

如果還沒有，請安裝Chrome瀏覽器
啟用開發者模式：
1. 打開Chrome
2. 前往 `chrome://extensions/`
3. 開啟右上角的「開發人員模式」

## 📁 專案結構設置

### 建立專案目錄
```
news-summary-extension/
├── frontend/              # Chrome插件前端
│   ├── manifest.json     # 插件配置檔
│   ├── popup.html        # 插件彈出視窗
│   ├── popup.js          # 前端邏輯
│   ├── styles.css        # 樣式檔案
│   └── icons/            # 插件圖示
├── backend/              # 後端API服務
│   ├── app.js            # 主程式入口
│   ├── routes/           # API路由
│   ├── services/         # 業務邏輯
│   ├── models/           # 資料模型
│   └── config/           # 配置檔案
├── docs/                 # 文件目錄
├── tests/                # 測試檔案
├── package.json          # 專案配置
└── README.md             # 專案說明
```

### 初始化專案
在終端機中執行：
```bash
# 建立專案目錄
mkdir news-summary-extension
cd news-summary-extension

# 初始化npm專案
npm init -y

# 建立目錄結構
mkdir frontend backend docs tests
mkdir frontend/icons backend/routes backend/services backend/models backend/config
```

## 🔧 開發工具配置

### VS Code 工作區設置
建立 `.vscode/settings.json`：
```json
{
  \"editor.formatOnSave\": true,
  \"editor.defaultFormatter\": \"esbenp.prettier-vscode\",
  \"emmet.includeLanguages\": {
    \"javascript\": \"javascriptreact\"
  },
  \"files.associations\": {
    \"*.js\": \"javascript\"
  }
}
```

### Prettier 設置
建立 `.prettierrc`：
```json
{
  \"semi\": true,
  \"trailingComma\": \"es5\",
  \"singleQuote\": true,
  \"printWidth\": 80,
  \"tabWidth\": 2
}
```

### Git 設置
建立 `.gitignore`：
```
# 依賴檔案
node_modules/
npm-debug.log*

# 環境變數
.env
.env.local

# 建置檔案
dist/
build/

# IDE設定
.vscode/
.idea/

# 作業系統檔案
.DS_Store
Thumbs.db

# 日誌檔案
*.log
```

## 📦 必要套件安裝

### 後端套件
```bash
cd backend

# 核心框架
npm install express cors helmet

# 工具庫
npm install axios cheerio puppeteer

# 開發工具
npm install --save-dev nodemon concurrently

# 資料庫 (選用)
npm install sqlite3 sequelize
```

### 前端套件（如果需要打包工具）
```bash
cd frontend

# 打包工具 (可選)
npm install --save-dev webpack webpack-cli html-webpack-plugin

# CSS框架 (可選)
npm install tailwindcss
```

## ⚡ 開發腳本設置

修改 `package.json`，新增開發腳本：
```json
{
  \"scripts\": {
    \"start\": \"concurrently \\\"npm run backend\\\" \\\"npm run frontend\\\"\",
    \"backend\": \"cd backend && nodemon app.js\",
    \"frontend\": \"cd frontend && live-server --port=3000\",
    \"test\": \"echo \\\"Error: no test specified\\\" && exit 1\"
  }
}
```

## 🧪 環境驗證

### 驗證清單
在開始開發前，確保以下項目都正常：

```bash
# 1. 檢查Node.js版本
node --version
# 應該顯示v16.x.x或更新版本

# 2. 檢查npm版本
npm --version
# 應該顯示8.x.x或更新版本

# 3. 檢查Git版本
git --version
# 應該顯示git version 2.x.x

# 4. 測試專案結構
ls -la
# 應該看到frontend、backend等目錄

# 5. 測試套件安裝
cd backend && npm list --depth=0
# 應該看到已安裝的套件清單
```

### 建立測試頁面
建立 `frontend/test.html` 測試開發環境：
```html
<!DOCTYPE html>
<html lang=\"zh-TW\">
<head>
    <meta charset=\"UTF-8\">
    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">
    <title>開發環境測試</title>
</head>
<body>
    <h1>🎉 開發環境設置成功！</h1>
    <p>如果你能看到這個頁面，表示環境設置正確。</p>

    <script>
        console.log('JavaScript正常運作');
        alert('環境設置完成！');
    </script>
</body>
</html>
```

## 🚨 常見問題解決

### 1. Node.js安裝問題
**症狀：** `node` 命令找不到
**解決方法：**
- 重新安裝Node.js
- 確認PATH環境變數設定正確
- 重新啟動終端機

### 2. npm套件安裝失敗
**症狀：** `npm install` 報錯
**解決方法：**
```bash
# 清除npm快取
npm cache clean --force

# 刪除node_modules重新安裝
rm -rf node_modules
npm install
```

### 3. Git設定問題
**症狀：** Git commit時要求設定用戶資訊
**解決方法：**
```bash
git config --global user.name \"你的姓名\"
git config --global user.email \"你的信箱\"
```

### 4. 權限問題（Mac/Linux）
**症狀：** 安裝套件時出現權限錯誤
**解決方法：**
```bash
# 使用nvm管理Node.js版本
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install node
nvm use node
```

## 🎯 下一步
環境設置完成後，接下來我們會開始實際編寫程式碼。確保所有工具都正常運作，這樣開發過程會更順暢！

---
*記住：花時間設置好開發環境，後面的開發會省很多時間！*